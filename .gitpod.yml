image:
  file: Dockerfile

tasks:
  - init: pyenv local 3.7.2 && pip install -e ".[dev]"
  - command: pip install -r requirements.pip

ports:
  - port: 6080
    onOpen: open-browser
  - port: 5900
    onOpen: ignore
  - port: 35900
    onOpen: ignore